# เชื่อม Orbit กับบัญชีบริษัท (Microsoft 365)

เอกสารนี้ใช้เปิด **โหมดทีม** — ให้ทุกคนล็อกอินด้วยบัญชีบริษัท
แล้วเห็นงานชุดเดียวกัน โดยข้อมูลอยู่ใน Microsoft 365 ของบริษัทเท่านั้น

ถ้ายังไม่ตั้งค่า Orbit จะทำงานแบบเดิม (เก็บข้อมูลในเบราว์เซอร์แต่ละเครื่อง)
ไม่มีอะไรพัง ไม่ต้องรีบ

---

## ภาพรวมของระบบ

```
เบราว์เซอร์ของแต่ละคน
   │  1. ล็อกอิน (Microsoft Entra ID)
   │  2. ได้ token
   ▼
Microsoft Graph ──► SharePoint ของบริษัท
                     └── /sites/Orbit/Shared Documents/Orbit/orbit-db.json
```

- **ไม่มีเซิร์ฟเวอร์ของเราเอง** ไม่มีค่าบริการรายเดือน
- **ข้อมูลไม่ออกนอก tenant** ของบริษัท ตัวเว็บเป็นไฟล์นิ่ง ๆ ที่ไม่เก็บอะไรเลย
- ใครเข้าถึงไฟล์ได้ ขึ้นกับสิทธิ์ของไซต์ SharePoint นั้นล้วน ๆ
  ระบบไม่ได้เปิดสิทธิ์ให้ใครเกินกว่าที่เขามีอยู่แล้ว

---

## สิ่งที่ต้องเตรียม

| อย่าง | ใคร |
|---|---|
| สิทธิ์สร้าง App registration ใน Entra ID | ผู้ดูแลระบบ (Global Admin / Application Admin) |
| สิทธิ์ให้ admin consent | ผู้ดูแลระบบ |
| ไซต์ SharePoint สำหรับทีม | ผู้ดูแลระบบ หรือเจ้าของไซต์ |

ใช้เวลาประมาณ 15–20 นาที ทำครั้งเดียวจบ

---

## ขั้นที่ 1 — ลงทะเบียนแอปใน Entra ID

1. เข้า <https://entra.microsoft.com> → **Identity** → **Applications** → **App registrations**
2. กด **New registration**
3. กรอก
   - **Name**: `Orbit`
   - **Supported account types**: `Accounts in this organizational directory only (Single tenant)`
   - **Redirect URI**: เลือกชนิด **Single-page application (SPA)** แล้วใส่ URL ที่จะใช้เปิดแอปจริง

   > สำคัญ: ต้องเป็นชนิด **SPA** เท่านั้น ถ้าเลือก Web จะเข้าไม่ได้
   > เพราะ Orbit ใช้ Authorization Code + PKCE ไม่มี client secret

4. กด **Register**
5. หน้าที่เด้งมาให้คัดลอกไว้สองค่า
   - **Application (client) ID**
   - **Directory (tenant) ID**

### Redirect URI ที่ต้องใส่

ใส่ให้ครบทุกที่ที่จะเปิดใช้จริง ตัวอย่าง

| ใช้ที่ไหน | ใส่ค่านี้ |
|---|---|
| GitHub Pages | `https://louissrichand.github.io/srichand-orbit/` |
| ทดสอบในเครื่อง | `http://localhost:8123/` |

ต้องมี `/` ปิดท้าย และต้อง**ตรงเป๊ะ** ตัวพิมพ์เล็กพิมพ์ใหญ่ก็สำคัญ
(ถ้าเข้าจาก `.../index.html` ระบบตัด `index.html` ให้เองแล้ว จึงลงทะเบียนแค่แบบมี `/` ปิดท้ายก็พอ)

---

## ขั้นที่ 2 — ให้สิทธิ์ Microsoft Graph

อยู่ในแอปที่เพิ่งสร้าง → เมนู **API permissions**

1. **Add a permission** → **Microsoft Graph** → **Delegated permissions**
2. เลือกสองอัน
   - `User.Read` — อ่านชื่อกับอีเมลของคนที่ล็อกอิน (ปกติมีมาให้แล้ว)
   - `Files.ReadWrite.All` — อ่าน/เขียนไฟล์ข้อมูลบน SharePoint
3. **Add permissions**
4. กด **Grant admin consent for &lt;ชื่อองค์กร&gt;** แล้วยืนยัน

> **Delegated** แปลว่าแอปทำได้เท่าที่ "คนที่ล็อกอิน" ทำได้เอง
> ไม่ใช่สิทธิ์ระดับแอปที่มองเห็นทั้งองค์กร ถ้าพนักงานคนหนึ่งไม่มีสิทธิ์เข้าไซต์นั้น
> เปิด Orbit ก็ยังเข้าไม่ได้อยู่ดี

---

## ขั้นที่ 3 — เตรียมที่เก็บข้อมูลบน SharePoint

1. สร้าง **Team site** เช่น `https://srichand.sharepoint.com/sites/Orbit`
2. เพิ่มสมาชิกทีมเข้าไซต์ ให้สิทธิ์ **Edit** (สมาชิกทุกคนต้องเขียนไฟล์ได้ ไม่งั้นแก้งานไม่ได้)
3. ในไลบรารี **Documents** สร้างโฟลเดอร์ชื่อ `Orbit`

ไฟล์ `orbit-db.json` จะถูกสร้างอัตโนมัติในโฟลเดอร์นั้นตอนที่มีคนล็อกอินคนแรก
ไม่ต้องสร้างเอง

> เปิด **Version history** ของไลบรารีไว้ด้วย จะได้ย้อนไฟล์ได้ถ้าเกิดอะไรขึ้น

---

## ขั้นที่ 4 — กรอกค่าลงใน `app/config.js`

ไฟล์นี้เป็นไฟล์เดียวที่ต้องแก้

```js
window.OrbitConfig = {
  tenantId: '00000000-0000-0000-0000-000000000000',   // Directory (tenant) ID
  clientId: '11111111-1111-1111-1111-111111111111',   // Application (client) ID

  siteHostname: 'srichand.sharepoint.com',
  sitePath: '/sites/Orbit',
  filePath: 'Orbit/orbit-db.json',

  pushDelayMs: 1500,
  pollMs: 15000
};
```

- ถ้าเว้น `siteHostname` / `sitePath` ไว้ ระบบจะไปเก็บใน **OneDrive ของแต่ละคน**
  ซึ่งไม่ได้แชร์กัน — ใช้ทดสอบได้ แต่ใช้งานจริงกับทีมไม่ได้
- **ทั้ง tenantId และ clientId ไม่ใช่ความลับ** เป็นเลขระบุตัวแอปที่เปิดเผยได้ตามปกติของ SPA
  ด่านกันจริงคือ (1) ต้องล็อกอินด้วยบัญชีใน tenant นี้ (2) ต้องมีสิทธิ์ในไซต์ SharePoint
  และ (3) Redirect URI ต้องตรงกับที่ลงทะเบียน จึงวางไว้ใน repo สาธารณะได้

---

## ขั้นที่ 5 — ทดสอบ

1. เปิดเว็บด้วย URL ที่ลงทะเบียนไว้เป๊ะ ๆ
2. ต้องเห็นหน้า **“เข้าสู่ระบบด้วยบัญชีบริษัท”**
3. กดเข้าสู่ระบบ → เด้งไปหน้า Microsoft → กลับมาที่ Orbit
4. มุมขวาบนต้องมีป้าย **“ซิงก์แล้ว”**
5. ทดสอบสองเครื่อง: เครื่อง A เพิ่มงาน ภายใน ~15 วินาที เครื่อง B ต้องเห็น

ตรวจไฟล์ได้ที่ SharePoint → `Documents/Orbit/orbit-db.json`

---

## ป้ายสถานะมุมขวาบน

| ป้าย | แปลว่า |
|---|---|
| กำลังโหลดข้อมูล… | กำลังดึงข้อมูลจากส่วนกลาง |
| กำลังบันทึก… | แก้แล้ว กำลังส่งขึ้น |
| ซิงก์แล้ว | ตรงกับส่วนกลางเรียบร้อย |
| ออฟไลน์ | เน็ตหลุด — ยังทำงานต่อได้ ค้างไว้ก่อน เน็ตกลับมาแล้วส่งเอง |
| ข้อมูลชนกัน | มีคนแก้พร้อมกัน ต้องเลือกว่าจะเก็บชุดไหน |
| ซิงก์ไม่สำเร็จ | มีปัญหาอื่น กดที่ป้ายเพื่อดูสาเหตุ |

### เมื่อข้อมูลชนกัน

ระบบเก็บฐานข้อมูลเป็นไฟล์เดียว จึงไม่รวมการแก้ไขให้อัตโนมัติ
ถ้าสองคนกดแก้พร้อมกันจริง ๆ ระบบจะ**หยุดแล้วถาม** ไม่ทับงานใครเงียบ ๆ
กดที่ป้ายแล้วเลือกได้สามทาง

1. **บันทึกงานของฉันเป็นไฟล์ก่อน** — ดาวน์โหลดสำรองไว้ แล้วค่อยตัดสินใจ
2. **ใช้ข้อมูลส่วนกลาง ทิ้งของฉัน** — ปลอดภัยที่สุด งานที่เพิ่งพิมพ์จะหาย
3. **เขียนทับส่วนกลางด้วยของฉัน** — งานของคนอื่นในรอบนั้นจะหาย

โอกาสเกิดต่ำ เพราะระบบดึงข้อมูลใหม่ทุก 15 วินาที
แต่ถ้าทีมโตเกิน ~20 คนแล้วเจอบ่อยขึ้น ค่อยขยับไปใช้ฐานข้อมูลจริง

---

## แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| `AADSTS50011: redirect URI does not match` | URL ที่เปิดไม่ตรงกับที่ลงทะเบียน ตรวจ `/` ปิดท้าย และ http/https |
| ขึ้น `AADSTS700016` หรือหาแอปไม่เจอ | `clientId` หรือ `tenantId` ผิด |
| ล็อกอินได้ แต่ขึ้น “บัญชีนี้ไม่มีสิทธิ์เข้าถึงที่เก็บข้อมูล” | ยังไม่ได้ให้ admin consent หรือคนนั้นไม่ได้เป็นสมาชิกไซต์ |
| ขึ้น “ไม่พบไซต์หรือโฟลเดอร์ที่ตั้งค่าไว้” | `siteHostname` / `sitePath` ผิด หรือยังไม่ได้สร้างโฟลเดอร์ |
| ไม่ขึ้นหน้าล็อกอินเลย | `clientId` ว่าง หรือเปิดไฟล์จากเครื่องแบบ `file://` (ต้องเปิดผ่าน http/https) |
| เปิดในหน้า Artifact แล้วไม่มีปุ่มล็อกอิน | ตั้งใจไว้แบบนั้น — Artifact บล็อกสคริปต์ภายนอก โหมดทีมใช้ที่ GitHub Pages |

---

## ข้อควรรู้ก่อนใช้จริง

- **สำรองข้อมูล** — เปิด Version history ของไลบรารี และกด “ดาวน์โหลดสำรอง” ในเมนูตั้งค่าเป็นระยะ
- **ขนาดทีม** — ออกแบบไว้สำหรับ 5–20 คน ไฟล์เดียวจบ เกินกว่านั้นควรย้ายไปฐานข้อมูลจริง
- **ปิดหน้าต่างระหว่างกำลังบันทึก** — เบราว์เซอร์จะเตือนก่อน อย่ากดปิดทิ้งตอนป้ายยังขึ้น “กำลังบันทึก…”
- **คนที่ยังไม่ล็อกอิน** กดปุ่ม “ลองใช้แบบเครื่องเดียวก่อน” ได้ ข้อมูลจะอยู่แค่ในเบราว์เซอร์นั้น
  และกลับเข้าโหมดทีมได้จากปุ่มในแถบซ้าย
