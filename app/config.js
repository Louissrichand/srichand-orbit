/* Orbit — ตั้งค่าเชื่อมบัญชีบริษัท
 *
 * ไฟล์นี้เป็นไฟล์เดียวที่ต้องแก้เพื่อเปิดโหมดทีม
 * ถ้า clientId ว่างไว้ ระบบจะทำงานแบบเดิมทุกอย่าง (เก็บข้อมูลในเบราว์เซอร์เครื่องเดียว)
 * จึงปลอดภัยที่จะ deploy ไฟล์นี้แบบว่างไว้ก่อน
 *
 * วิธีกรอก ดูขั้นตอนใน SETUP-M365.md
 */
window.OrbitConfig = {

  // ชื่อองค์กรที่แสดงในหน้าตั้งค่า ไม่มีผลกับการเชื่อมต่อ
  orgName: 'Srichand',

  /* ---- Microsoft Entra ID ---- */

  // Directory (tenant) ID จากหน้า App registration
  tenantId: '',

  // Application (client) ID จากหน้าเดียวกัน
  // เว้นว่าง = ปิดโหมดทีม ใช้งานแบบเครื่องเดียวเหมือนเดิม
  clientId: '',

  /* ---- ที่เก็บข้อมูลกลาง (SharePoint ของบริษัท) ---- */

  // โดเมน SharePoint เช่น 'srichand.sharepoint.com'
  siteHostname: '',

  // เส้นทางไซต์ที่จะเก็บไฟล์ เช่น '/sites/Orbit'
  // ถ้าเว้นว่างจะใช้ OneDrive ของผู้ใช้แทน (ไม่แชร์กับทีม เหมาะกับการทดสอบ)
  sitePath: '',

  // ที่อยู่ไฟล์ในไลบรารีเอกสาร
  filePath: 'Orbit/orbit-db.json',

  /* ---- พฤติกรรมการซิงก์ ---- */

  // หน่วงก่อนส่งขึ้น หลังหยุดแก้ (มิลลิวินาที)
  pushDelayMs: 1500,

  // ระยะเวลาถามหางานใหม่จากเพื่อนร่วมทีม (มิลลิวินาที)
  pollMs: 15000
};
